//find number of odd and even digits from given inputted number
#include<stdio.h>
#include<conio.h>

int main()
{
    int i[10]=0,even=0,odd=0;
    int *ptr;
    printf("Enter to find numbers of even and odd numbers");
    scanf("%d",&i);

    for(ptr=0;ptr<i;ptr++)
    {
      if(i%2=0)
      {

      }
    }
    printf("\n Sum of 10 numbers is = %d \n",sum);
    printf("\n-----------------------------------------------\n");
}
